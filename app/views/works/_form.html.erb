<%= form_with(model: work) do |form| %>
  <% if work.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(work.errors.count, "error") %> prohibited this work from being saved:</h2>

      <ul>
        <% work.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.select :user_id,User.all.pluck(:name,:id) %>
  </div>

  <div>
    <%= form.label :work_name, style: "display: block" %>
    <%= form.text_field :work_name %>
  </div>

  <div>
    <%= form.label :work_location, style: "display: block" %>
    <%= form.text_field :work_location %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
